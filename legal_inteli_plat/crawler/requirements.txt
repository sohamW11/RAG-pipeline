# Crawler microservice runtime dependencies.
# Core versions are pinned to what the service is developed and tested against.

# --- Web / API ---
fastapi==0.139.0
uvicorn[standard]>=0.30,<1.0
starlette==1.3.1

# --- Config / validation ---
pydantic==2.13.4
PyYAML==6.0.3

# --- HTTP + HTML crawling ---
httpx==0.28.1
anyio==4.14.1
beautifulsoup4==4.15.0
soupsieve==2.8.4
# Optional JS-rendered listings (crawler.crawler.fetcher.PlaywrightFetcher).
playwright>=1.44,<2.0

# --- Persistence ---
SQLAlchemy==2.0.51
greenlet==3.5.3
aiosqlite==0.22.1
# Async PostgreSQL driver for production (sqlite+aiosqlite used locally/tests).
asyncpg>=0.29,<1.0
alembic>=1.13,<2.0

# --- Messaging / cache (optional; disabled by default) ---
aiokafka>=0.11,<1.0
redis>=5.0,<6.0

# --- Reliability / observability ---
tenacity==9.1.4
structlog==26.1.0

# --- Testing ---
pytest==9.1.1
pytest-asyncio==1.4.0
